fn testWhile() {
    var idx i32 := 0

    while idx < 10 {
        printInt(i)     // TODO should throw error "i is not defined"
        idx = idx + 1
    }

    while false {
        printStr("dead code")
    }

    idx = 0
    while true {
        printStr("endless loop")

        if idx >= 30 {
            printStr("aborted endless loop")
            exit(0)       // TODO break
        }
        idx = idx + 1
    }
}

fn testFor() {
    // TODO: more readable
    for var i i32 := -4  i <= 86  i = i + 6 {
        printInt(i)
    }
}

fn main() {
    printStr("for tests")
    testFor()
    printStr("------------")
    printStr("while tests")
    testWhile()
}
