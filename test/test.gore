var idx i32 := 86
var testStr str := "test string"

fn main() {
    printInt(idx)
    idx = 420
    printInt(idx)

    printStr("------------")

    printStr(testStr)
    testStr = "new string"
    printStr(testStr)

    printStr("------------")

    printInt(idx)
    idx = 6 + 20 * 2 * 2
    printInt(idx)
    idx = idx - 10
    printInt(idx)
}
