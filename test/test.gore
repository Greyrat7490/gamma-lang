var v2 i32 := 420

fn test_fn(v1 i32) {
    printInt(v1)
    printInt(v2) // r9 will be overwritten here (TODO: push if later used)
    printInt(v1)
}

fn test_fn2(v1 str) {
    printStr(v1)
    printStr("test fn2: printStr")
    printStr("test fn2: printStr")
    printStr("test fn2: printStr")
}

fn main() {
    printInt(v2)  // global variables don't work yet
    test_fn2("test fn2: arg string")
    test_fn(86)
}
