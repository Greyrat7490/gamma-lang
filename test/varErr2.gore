var v1 i32 := 86
var v1 i32 := 69                // error already used name

fn main() {
    printInt(v1)
    printStr(",")
    printInt(v2)                // would cause an error (but does not get reached)
}
