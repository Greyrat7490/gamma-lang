var v1 i32 := 86
var v2 str := "some string"

fn test1(a1 i32) {
    printInt(a1)
    printInt(v1)
    printInt(a1) // r9 will be overwritten by the print before (TODO: push and pop if used later)
}

fn test2(a1 str) {
    printStr(a1)
    printStr(v2)
    printStr(a1) // same here with r9 and r10
}

fn main() {
    test1(420)
    test2("some arg")
}
