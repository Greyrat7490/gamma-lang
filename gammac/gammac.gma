import "io.gma"
import "parse/parse.gma"

fn main(args [$]*char) {
    if args.len < 2 {
        printStr("[ERROR] no source file provided\n")
        exit(1)
    }
    src_path := from_cstr(args[1])

    // set import dir

    Parse(src_path)

    // show AST

    // type check

    // gen asm

    // gen exe

    // run
}
