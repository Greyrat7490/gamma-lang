package types

// TODO to array
// TODO in work
// var resolvedInfers map[uint64]Type = make(map[uint64]Type)
// 
// func ResolveInfer(t Type) Type {
//     if inferType,ok := t.(InferType); ok {
//         if resolvedType := resolvedInfers[inferType.Idx]; resolvedType != nil {
//             return resolvedType
//         }
//     }
// 
//     return nil
// }
