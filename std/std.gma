import "syscall.gma"
import "io.gma"
import "socket.gma"


// tmp only for testing
fn printArr(arr [2][2][2]i32) {
    for i i32, 2 {
        printStr("{ ")
        for j i32, 2 {
            printStr("{ ")
            for k i32, 2 {
                printInt(arr[i][j][k]) printStr(" ")
            }
            printStr("} ")
        }
        printStr("}\n")
    }
}
